#include<iostream>
using namespace std;
int main(){
    int n1,n2,sum;
    cout<<"first number:";// no ending of the line==endline for end line use<<endl
    cin>>n1;
    cout<<"second number:"<<endl;
    cin>>n2;
    sum=n1+n2;
    cout<<"the sum is "<<sum;// space in the rujn time is due to the whitespace kept inside the double quotes
    return 0;
}